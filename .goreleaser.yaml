version: 2

# .goreleaser.yaml — see https://goreleaser.com/customization/
# Goreleaser v2 schema. Cross-compiles darwin+linux × amd64+arm64.
# CGO_ENABLED=0 is the visible expression of the no-cgo constraint
# (DEC-001 — pure-Go modernc.org/sqlite driver).

before:
  hooks:
    - go mod tidy

builds:
  - id: brag
    main: ./cmd/brag
    binary: brag
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}

archives:
  - id: default
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    formats: [tar.gz]
    files:
      - README.md
      - LICENSE
      - CHANGELOG.md

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

changelog:
  use: github
  sort: asc
  groups:
    - title: Features
      regexp: '^.*?feat(\(.+?\))??!?:.+$'
      order: 0
    - title: Bug fixes
      regexp: '^.*?fix(\(.+?\))??!?:.+$'
      order: 1
    - title: Build / CI
      regexp: '^.*?(build|ci|chore)(\(.+?\))??!?:.+$'
      order: 2
    - title: Documentation
      regexp: '^.*?docs(\(.+?\))??!?:.+$'
      order: 3
    - title: Other
      order: 999

# Binary Homebrew FORMULA (not a cask) on the shared jysf tap — DEC-040.
# Formulae are not Gatekeeper-quarantined, so no code-signing/notarization is
# required (a cask would be). `brews:` emits a goreleaser deprecation warning;
# we accept it deliberately (DEC-040) — a cosmetic warning beats a real signing
# cost. If goreleaser removes `brews:`, hand-maintain the formula in the tap.
brews:
  - name: bragfile
    # Write into Formula/ (matches crustyimg on this shared tap); goreleaser's
    # brews default is the repo root, which would split the tap layout.
    directory: Formula
    repository:
      owner: jysf
      name: homebrew-tap
      branch: main
      # PAT must have Contents:read/write on jysf/homebrew-tap (the shared tap),
      # not just the old homebrew-bragfile — the 403 that broke the v0.5.2 cut.
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: "https://github.com/jysf/bragfile000"
    description: "Local-first Go CLI to capture and retrieve career-worthy moments."
    license: "MIT"
    skip_upload: auto
    test: |
      system "#{bin}/brag", "--version"
    install: |
      bin.install "brag"

release:
  github:
    owner: jysf
    name: bragfile000
  prerelease: auto
  draft: false
