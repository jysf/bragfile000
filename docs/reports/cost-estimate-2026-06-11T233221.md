# bragfile — build cost estimate

*Generated 2026-06-11T23:32:21-0700. Covers the project from first commit
(2026-04-19) through the most recent (2026-06-11), i.e. PROJ-001 (MVP) plus
PROJ-002 through STAGE-007 in progress.*

> **Caveat:** per-cycle token usage was never logged ("track cycle
> duration/cost in the spec" was a rejected round-1 process-feedback item),
> and the available RTK telemetry is global across all projects, not
> bragfile-specific. The volumetrics and timeline below are hard facts; the
> dollar figure is a transparent model, not a metered total.

## Hard facts (measured)

| Metric | Value |
|---|---|
| Wall-clock span | Apr 19 → Jun 11 2026 (~54 days) |
| Active days (distinct commit dates) | 21 |
| Commits | 148 (30 design, 20 build, 7 ship, 18 feat, 53 chore, 7 frame, 7 docs, 4 fix, 1 refactor) |
| Merged PRs | 41 |
| Shipped specs / stages / DECs | 31 / 7 / 20 |
| Go code (non-test) | 5,117 LOC |
| Go tests | 14,409 LOC (2.8x the impl) |
| Spec/stage/brief/DEC markdown | 45,313 lines |
| Total tracked lines | 71,879 |

## Time / effort

The project's own `docs/framework-feedback/process-feedback.md` clocks
**~2.5–3 hrs per spec** (design + build + verify + ship across fresh
sessions).

- 31 specs x 2.5–3h ≈ **78–93 agent-hours**, plus stage frames/ships,
  project frame/design/ship, weekly reviews, the distribution spec, and the
  learning session → **~90–120 hours** of Claude session time.
- Over 21 active days that's ~4.5–5.5 hrs/active day — consistent with the
  commit clustering (28 commits on Apr 20, 23 on Apr 25).

## Dollar cost — two framings

### A. API-equivalent (metered) — estimate

~150 model sessions (124 spec-cycles + ~26 stage/project/review sessions),
agentic Opus-tier usage. Modeling output against the final artifact (~65K
authored lines ≈ 0.5M output tokens) plus 4–8x generation overhead
(reasoning, discarded edits, reflections, PR bodies) and the input-heavy
resend pattern of agentic loops (mitigated by prompt caching):

- **≈ $250–$500** on Opus pricing ($15/M in, $75/M out).
- If much of the build ran on **Sonnet**, divide by ~4–5 → **~$60–$120**.

### B. Subscription (actual cash outlay) — likely the real number

If built with Claude Code under a Max/Pro plan rather than raw API, the
marginal cost was the **flat subscription** over the ~2-month span — roughly
**$200–$400 total** regardless of tokens; the metered estimate above is "what
it would have cost à la carte."

## The honest read

For **~19.5K lines of tested Go + 20 design decisions + a full distribution
pipeline (goreleaser / Homebrew / CI)**, the ballpark is **a few hundred
dollars and ~100 hours of agent time over 2 months** — reconstructed from
volumetrics and the project's documented pace, not a logged ledger.

For a *real* number going forward, add a per-cycle token/cost line to Build
Completion (reviving the rejected round-1 recommendation); then the next
"what did this cost" is a sum, not an estimate.

---

### Method notes

- Span / commits / PRs / counts: `git log`, `gh pr list`, directory counts.
- LOC: `wc -l` over tracked `*.go` (split test vs non-test) and
  `projects/**/*.md` + `decisions/*.md`.
- Pace anchor: `docs/framework-feedback/process-feedback.md` ("~2.5–3 hours
  per spec").
- Token model: final-artifact line count x ~8 tokens/line as an output
  floor, x4–8 agentic overhead, priced at published Opus/Sonnet rates. No
  per-session billing data was available; treat as an order-of-magnitude
  estimate.
